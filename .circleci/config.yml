version: 2.1

workflows:
  version: 2
  simple-trigger:
    jobs:
      - trigger-with-curl

jobs:
  trigger-with-curl:
    docker:
      - image: curlimages/curl:latest
    steps:
      - run:
          name: Direct API Trigger
          command: |
            echo "🚀 Direct API trigger for 10invisible..."
            curl -X POST \
              "https://www.kaggle.com/api/v1/kernels/solomonoffor/10invisible/versions" \
              -H "Authorization: Bearer ${KAGGLE_KEY}" \
              -H "Content-Type: application/json" \
              -d '{}'
            echo "✅ Trigger sent - check Kaggle for new version!"
